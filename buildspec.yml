version: 0.2

phases:
  build:
    commands:
      - echo Entered the build phase...
      - mvn clean package
  post_build:
    commands:
      - echo Build completed successfully
      - mv target/userservice.jar ./
artifacts:
  files:
    - userservice.jar
  discard-paths: yes